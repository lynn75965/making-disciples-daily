// src/constants/growthSigns.ts
// Making Disciples Daily -- SSOT: Growth Signs (the 7)
//
// Owns the VALUES of the `growth_sign` enum (type imported from contracts.ts).
// Growth assessments, the timeline, and the readiness flag are data-driven from
// this list (Phase 2). The DB enum mirrors these exactly.
//
// STUB STATE (Phase 0): empty until Phase 2 populates the seven growth signs.

import type { GrowthSign } from './contracts';

export const GROWTH_SIGNS: readonly GrowthSign[] = [];
