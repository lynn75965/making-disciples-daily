// src/constants/validation.ts
// Making Disciples Daily -- SSOT: Validation Rules
//
// Owns shared validation limits and rules (field lengths, formats) used by both
// forms and -- where mirrored -- backend checks. Frontend is authoritative.
//
// STUB STATE (Phase 0): empty until the first forms land in Phase 1.

export const VALIDATION = {} as const;
