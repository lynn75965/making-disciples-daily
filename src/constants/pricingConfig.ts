// src/constants/pricingConfig.ts
// Making Disciples Daily -- SSOT SLOT RESERVED (intentionally empty)
//
// Payments / billing are EXCLUDED from v1 (locked decision #4). Stripe is
// deferred; when added it follows the BLS webhook-from-SSOT pattern. This file
// exists only to reserve the SSOT slot. Do NOT add pricing tiers or entries.
// Leave empty until Lynn explicitly opens this scope.

export {};
